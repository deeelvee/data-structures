const {Stack} =  require('./structures/stack');
const {HashMap} =  require('./structures/hashmap');
const {DoublyLinkedListNode, DoublyLinkedList} =  require('./structures/doubly-linked-list');
const {ArrayQueue, NaiveArrayQueue, LinkedListQueue} =  require('./structures/queue');
const {SinglyLinkedList, SinglyLinkedListNode} =  require('./structures/singly-linked-list');
const {PrefixTree, PrefixTrieNode} =  require('./structures/prefix-tree');
const {BinaryTree, BinaryTrieNode} =  require('./structures/binary-tree');
const {BinarySearchTree} =  require('./structures/binary-search-tree');
const {MaxHeap, MinHeap} = require('./structures/heap');

module.exports = {
    Stack,
    HashMap,
    DoublyLinkedListNode,
    DoublyLinkedList,
    NaiveArrayQueue,
    ArrayQueue,
    LinkedListQueue,
    SinglyLinkedList,
    SinglyLinkedListNode,
    PrefixTree,
    PrefixTrieNode,
    BinaryTree,
    BinarySearchTree,
    BinaryTrieNode,
    MaxHeap,
    MinHeap,
}